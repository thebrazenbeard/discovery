# Discovery — Exodus Reconstruction V1

Status: **STARTING_SNAPSHOT — FRESHNESS REQUIRED BEFORE EFFECT**

Discovery is a portfolio architecture laboratory, not a permanent ChatGPT worker and not a universal framework.

A fresh runtime does not need the retired conversation that created the current source candidate.

## Purpose

Discovery asks:

> Which mechanics genuinely deserve reuse across the portfolio, and which should remain independently evolved?

It uses a full portfolio census, public/private-safe classification, falsifiable relationship edges, bounded experiments, rejection conditions, and hostile review.

## Durable source

Primary repository:
`thebrazenbeard/discovery`

Current workstream at this snapshot:
- branch: `vera/discovery-shared-substrate-v1`
- Draft PR: `#1`

Fresh-check the branch and PR before relying on any head recorded in a continuation checkpoint.

## Reconstruction sequence

A fresh runtime should:

1. Read `README.md`.
2. Read `docs/DISCOVERY_PROTOCOL_V1.md`.
3. Read `docs/CLASSIFIER_PROTOCOL_V1.md`.
4. Read `portfolio/PORTFOLIO_CENSUS_V1.json`.
5. Read `portfolio/PUBLIC_RELATIONSHIP_GRAPH_V1.json`.
6. Enumerate current candidate records under `candidates/`.
7. Read experiment records under `experiments/`.
8. Run `python tools/validate_discovery.py` and the repository test suite.
9. Fresh-check current exact source heads for any candidate before changing evidence state or running an experiment.
10. Fresh-check open stacked PRs whose bases are this branch; do not carry review/PASS state across head movement.
11. Use the current Bus topology for work-bearing coordination; do not require a ChatGPT conversation URL.

## Authority

Discovery source work may propose and test abstractions.

Discovery does not grant:
- merge or canonical promotion;
- deployment or installation;
- downstream migration/deletion;
- provider or credential changes;
- project authority;
- semantic ownership transfer.

Patrick's exact authority remains required where those effects are protected.

## Current experiment posture

Runner ↔ WIP:
- broad shared execution/recovery state-machine hypothesis was narrowed;
- current surviving hypothesis is only a small effect-integrity interchange seam;
- scheduling, leases/fencing, budgets, workspace checkpoints, lifecycle, authority, and completion semantics remain project-owned.

World Zero ↔ Mosaic:
- pairwise shared executable-harness hypothesis rejected;
- harnesses remain project-specific;
- only a weaker generic receipt pattern remains `NEEDS_MORE_EVIDENCE`;
- require a third materially independent consumer before proposing a shared schema/component.

HC Brain ↔ Transcendence:
- exact base+overlay reconstruction remains a hypothesis;
- refresh exact heads and remeasure before carrying old overlap counts.

Rezon ↔ Runner:
- complementary reasoning/execution boundary remains a hypothesis;
- select exact Rezon candidate subject before experimentation.

## Privacy hold

This public branch historically introduced a candidate filename containing a private project identifier.

The file contents were sanitized, but removing the historical/public filename would require a deletion/history-remediation decision not authorized by the Exodus itself.

Do not expand that private identity further in public Discovery material.

Record any approved remediation as a distinct privacy effect with exact scope and evidence.

## Chat-dependency rule

No candidate, experiment, classification, or future worker may depend on:
- this retired chat;
- a chat title or conversation ID;
- a ChatGPT URL;
- remembered tab state;
- a permanent worker chat.

Temporary execution environments are terminals only.

## Coordination owner after Exodus

Primary future interface: `Vera`.

Engineering tasks may be delegated through durable GitHub/Bus state to ephemeral workers or through `BT2 Coordinator` where appropriate.

Control-plane/install/provider work belongs to `Vera Control Plane Coordinator`.

## Next safe frontier

Continue evidence-producing experiments, not framework construction.

The strongest next candidate is either:
- exact HC Brain ↔ Transcendence base+overlay reconstruction, or
- a third-consumer test of the generic experiment-receipt pattern.

Choose by fresh evidence and collision state, not by this snapshot alone.
